[package]
name = "coppo-addons"
version = "0.0.1-alpha"
edition = "2021"

[dependencies]
clap = { version = "4.5.7", features = ["cargo", "color", "help"] }
coppo-config = { path = "../coppo-config" }